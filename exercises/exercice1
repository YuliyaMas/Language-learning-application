A named #entity# is a real-world object, such as persons, locations, #organizations#, etc.
Recursion are mainly of #two# types
The first one is called #direct# recursion and another one is called #indirect# recursion